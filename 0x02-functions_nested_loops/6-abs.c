#include <stdio.h>
#include "main.h"

/**
 * _abs - check the code for ALX students
 * @n: int argument for function
 * Return: Always 0
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
return (n * -1);
}
