#include <stdio.h>
#include "main.h"

/**
* mul - multiplication of two variables
* @a: the parameter the int will use to check for the argument of the function
* @b: the parameter the int will use to check for the argument of the function
* Return: Always 0
*/
int mul(int a, int b)
{
return (a * b);
}
