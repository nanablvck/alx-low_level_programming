#include "main.h"
#include <stdio.h>
/**
 * _strlen - Code to print the length of a string
 * @s: Int value for the argument of the function
 * Return: Always 0
 */

int _strlen(char *s)
{
int c;

for (c = 0; s[c] != 0; c++)
{
}
return (c);
}
